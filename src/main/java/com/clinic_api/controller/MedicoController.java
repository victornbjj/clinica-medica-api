package com.clinic_api.controller;

public class MedicoController {

    ///
//    @GetMapping("/medicos")
//    public List<?> listarMedicos() {
//        Role role = // obtenha o papel do usuário autenticado
//                List<Medico> medicos = medicoService.findAll();
//
//        if (role == Role.ADMIN) {
//            return medicos.stream().map(MedicoAdminResponse::new).toList();
//        } else if (role == Role.MEDICO) {
//            return medicos.stream().map(MedicoAdminResponse::new).toList();
//        } else if (role == Role.CLIENTE) {
//            return medicos.stream().map(MedicoPublicResponse::new).toList();
//        } else {
//            throw new AccessDeniedException("Acesso negado");
//        }
//    }
//    ///
}
